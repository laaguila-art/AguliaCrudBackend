# Example environment for Aiven MySQL
# Copy this file to .env and fill in your real values

# App
PORT=3000

# Database (Aiven)
DATABASE_HOST=mysql-xxxxxx-xxxxx.aivencloud.com
DATABASE_PORT=18081
DATABASE_USER=avnadmin
DATABASE_PASSWORD=REPLACE_ME
DATABASE_NAME=defaultdb
# Optional: use existing table names instead of defaults
# If your existing DB uses different table names, set these:
# USERS_TABLE=existing_users
# POSITIONS_TABLE=existing_positions
# SSL settings (Aiven requires SSL)
DATABASE_SSL_MODE=REQUIRED
# Path to the downloaded CA cert file (PEM). Put it in the project root or any path you prefer.
# Example: ./aiven-ca.pem
DATABASE_SSL_CA_PATH=

# JWT
JWT_SECRET=supersecret_access
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=supersecret_refresh
JWT_REFRESH_EXPIRES_IN=7d
